{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "private": true,
  "description": "Pentaho Visual Samples D3-Bar",
  "license": "MIT",

  "packages": [
    {
      "name": "bar-d3",
      "main": "model"
    }
  ],

  "config": {
    "pentaho/service": {
      "${project.artifactId}_${project.version}/bar-d3": "pentaho/visual/base"
    }
  },

  "dependencies": {
    "d3": "^${d3.version}"
  }
}
