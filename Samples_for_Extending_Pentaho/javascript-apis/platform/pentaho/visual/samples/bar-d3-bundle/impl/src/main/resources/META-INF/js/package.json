{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "private": true,
  "description": "Pentaho Visual Samples D3-Bar",
  "license": "MIT",

  "config": {
    "pentaho/typeInfo": {
      "${project.artifactId}_${project.version}/model": {"base": "pentaho/visual/base/model"}
    },
    "pentaho/instanceInfo": {
      "${project.artifactId}_${project.version}/config": {"type": "pentaho.config.spec.IRuleSet"}
    }
  },

  "dependencies": {
    "d3": "^${d3.version}"
  }
}
